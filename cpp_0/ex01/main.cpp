//
// Created by arnisfet on 27.01.2022.
//

#include "header.hpp"

int main(void)
{
	using std::cout;
	using std::cin;
	int run;
	std::string first;

	run = 1;
	while (run)
	{
		cout << "> ";
		std::getline(cin, first);
		if (first == "ADD")
		{

		}
		else if (first == "SEARCH")
		{

		}
		else if (first == "EXIT")
		{

		}
	}
}